# Padroes_CygnusForge

Stack: CSharp, React

Este projeto usa o cygnus-harness, instalado globalmente em `~/.claude` nesta
maquina (agentes, skills, hooks, steering â€” ver `~/.claude/CLAUDE.md` para o fluxo
de trabalho completo). Este arquivo so guarda o que e especifico deste projeto.

Estado especifico deste projeto (gerado por `bootstrap.ps1`, nao pela instalacao
global): `.specs/`, `.claude/harness-config.json`, `.claude/knowledge/`,
`.specs/PROGRESS.md`.